{
  "name": "verse-rinkeby-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ verse-rinkeby-subgraph",
    "create-local": "graph create --node http://localhost:8020/ verse-rinkeby-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ verse-rinkeby-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 verse-rinkeby-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1",
    "ethers": "^5.5.4"
  }
}
